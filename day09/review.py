# variable(변수)
# data_type(데이터 타입)
# ** 기본 타입: int, float, bool, str
# ** 참조 타입: list, dict, set, ···
# operator(연산자): +, -, *, in, ···
# condition(조건문): if, elif, else
# loop(반복문): for, while
# def(함수) abc():return

